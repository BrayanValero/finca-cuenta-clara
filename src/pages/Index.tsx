
import Dashboard from "./Dashboard";

const Index = () => {
  // Por ahora, simplemente renderizamos el Dashboard directamente
  // Más adelante, esto se integrará con la autenticación de Supabase
  return <Dashboard />;
};

export default Index;
